{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "dev:tribe-neon",
      "type": "shell",
      "command": "npx next dev --hostname 127.0.0.1 --port 3000",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    },
    {
      "label": "build:tribe-neon",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": []
    }
  ]
}
